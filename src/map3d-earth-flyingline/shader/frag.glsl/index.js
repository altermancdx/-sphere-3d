const Cube = require('@/Cube')(module, module.exports, require);
Cube("datav:/com/map3d-earth-flyingline/0.0.14/shader/frag.glsl",[],function(a){return a.exports="#ifdef GL_ES\n  precision highp float;\n#define GLSLIFY 1\n#endif\n\nvarying vec2 vUv;\nuniform vec3 uColor;\nuniform float uLength;\nuniform float uOpacity;\nuniform float uTimeCounter;\nuniform sampler2D uTexture;\n\nfloat calculateTimeControlOpacity(float timeCounter, vec2 vUv){\n  float uvGap = uLength;\n  float uvTail = timeCounter - uvGap;\n\n  if((uvTail < vUv.x) && (vUv.x < timeCounter)) {\n    float opacity = (1.0 - (timeCounter - vUv.x) / uvGap);\n    opacity = pow(opacity, 1.5);\n    return opacity;\n  } else {\n    return 0.0;\n  }\n}\n\nvoid main() {\n  float currentOpacity = calculateTimeControlOpacity(uTimeCounter, vUv);\n  gl_FragColor = vec4(uColor, currentOpacity * uOpacity);\n}",a.exports});;
